generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id          String   @id @default(uuid())
  email       String   @unique
  full_name   String?
  role        String   @default("user")
  plan        String   @default("semente")
  status      String   @default("active")
  photo_url   String?  @db.Text
  last_login  DateTime @default(now())
  created_at  DateTime @default(now())

  @@map("users")
}

model SchoolClass {
  id        String   @id @default(uuid())
  name      String
  grade     String
  shift     String
  teacherId String
  createdAt DateTime @default(now())

  @@map("school_classes")
}

model Student {
  id              String   @id @default(uuid())
  name            String
  classId         String
  photo_url       String?  @db.Text
  performance_logs Json?    // Armazena as previsões de IA e BNCC
  created_at      DateTime @default(now())

  @@map("students")
}

model Occurrence {
  id          String   @id @default(uuid())
  studentId   String
  type        String   // Ex: Disciplinar, Pedagógica
  description String   @db.LongText
  severity    String   @default("low")
  created_at  DateTime @default(now())

  @@map("occurrences")
}

model Tutoria {
  id          String   @id @default(uuid())
  studentId   String
  notes       String   @db.LongText
  bncc_codes  String?  @db.Text // Códigos da BNCC trabalhados
  date        DateTime @default(now())

  @@map("tutorias")
}
